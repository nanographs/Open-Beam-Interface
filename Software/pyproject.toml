[project]
name = "OBI-Software"
version = "0"
description = "Software interface for Open Beam Interface applet"
authors = [
    {name = "isabelburgos", email = "isabel@nanographs.io"},
]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"
license = {text = "0BSD OR Apache-2.0"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
